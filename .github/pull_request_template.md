## What / why

## Checklist

- [ ] `swift test` passes
- [ ] If this touches `PrivateDisplayAPI.swift` or the restore logic in `BlackoutController.swift`: verified on real hardware (`--verify-restore`, see docs/HOST_VERIFICATION.md), and stated which Mac model / macOS build below
- [ ] Adding a host to `shippedAllowlist`: linked the verification report issue

## Tested on

Mac model / macOS build:
